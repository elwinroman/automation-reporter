LOGS_DIRECTORY=
# Comma-separated list of allowed CORS origins. Leave empty to allow all origins (*).
ALLOWED_URLS=

# Frontend — base URL del servidor nginx que sirve los reportes HTML.
# Ejemplo: http://10.4.88.10:9001
# Configurable en frontend/.env.local como VITE_REPORTS_BASE_URL
